
export declare global {
    interface Window {
        dataLayer: [],
        gtag: (
            command: string,
            target: string | Date,
            params: Record<key, st>

        ) => void

    }
}